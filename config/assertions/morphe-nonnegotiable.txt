# Patches that must be applied for the Vantage M (Morphe) build, one per line.
# This variant uses the Morphe default set, without comment hiding or Return
# YouTube Username. Count isn't checked since the default set grows.
#
# Note: the standalone "Video ads" patch was merged into "Hide ads" in
# morphe-patches 1.34.0 (it hides video ads by default). "Hide ads" below is the
# ad-blocking guard now, matching the youtube/alt/music non-negotiable lists.
#
# Note: "Change package name" was renamed to "Clone app" in morphe-patches
# 1.39.0 (MorpheApp/morphe-patches#2347). Same patch, same option keys
# (packageName/updatePermissions/updateProviders), but it now defaults to
# disabled - so a stale name here means it is silently skipped and "GmsCore
# support" renames the package to its own fallback (app.morphe.android.youtube),
# breaking in-place updates for everyone on app.vantage.youtube.morphe.
Hide ads
SponsorBlock
Return YouTube Dislike
Alternative thumbnails
Hide Shorts components
Disable Shorts resuming on startup
Change start page
Remove background playback restrictions
Custom branding
Clone app
GmsCore support
