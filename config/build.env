# Build config, sourced by scripts/*.sh.

# morphe-cli is pinned (the patch format is coupled to the cli version).
# Keep the cli at least as new as what MorpheApp pairs with the current patches
# release; a bundle that needs a newer patcher API dies at `list-versions` with a
# NoClassDefFoundError for the missing class. Known breaks:
#   1.9.1  cannot load morphe-patches 1.36.0+ (PathOptionsKt)
#   1.12.0 cannot load morphe-patches 1.40.0+ (AvailabilityResolver, first
#          shipped in cli 1.13.1 - verified by grepping the -all.jar)
MORPHE_CLI_VERSION=1.14.0
MORPHE_CLI_REPO=MorpheApp/morphe-cli

# Patch sources. anddea is my fork with the setting defaults baked in.
ANDDEA_REPO=pmaxhogan/revanced-patches
ANDDEA_CHANNEL=release
MORPHE_PATCHES_REPO=MorpheApp/morphe-patches
MORPHE_CHANNEL=release

YT_PACKAGE=com.google.android.youtube
MUSIC_PACKAGE=com.google.android.apps.youtube.music

# Piko is the morphe patch set for Twitter/X; it ships .mpp bundles like anddea.
# We consume a fork of piko that
# adds the Vantage-only patches ("Hide verified users", "Daily time limit").
# The fork syncs nightly with upstream and rebuilds its .mpp on every push, so
# this floats to the fork's latest (non-prerelease) release.
PIKO_REPO=pmaxhogan/piko
PIKO_CHANNEL=release
X_PACKAGE=com.twitter.android

# x-shim (inotia00's compatibility layer) was required to patch X 11.88-12.4.
# From X 12.5.0 upstream piko patches the app on its own (piko README), and the
# build now targets 12.19.1+, so the shim bundle is gone from the build. If a
# future X version ever needs a shim again, re-add a pinned, sha256-verified
# second bundle here and stack it in build-x.sh (git history has the shape).

# Empty = newest patch-compatible version (morphe-cli list-versions). Set to pin.
YT_VERSION_PIN=
MUSIC_VERSION_PIN=
X_VERSION_PIN=

# YouTube has a universal APK; Music is arm64-v8a only.
YT_ARCH=universal
MUSIC_ARCH=arm64-v8a

# Sources tried on a cache miss, in order. apkmirror/apkpure use curl_cffi to get
# past Cloudflare; apkcombo/aptoide are curl fallbacks.
DL_SOURCES="apkmirror apkpure apkcombo aptoide"

# X has no universal single APK - it is distributed as a split APKM, which morphe
# patches directly. Only APKMirror serves the genuine APKM, so it is the sole
# source; the download gate verifies every split's signing cert (see download-apk.sh).
X_DL_SOURCES="apkmirror"
X_ARCH=universal

STOCK_CACHE_TAG=stock-cache

# ---------------------------------------------------------------------------
# Claude clones - several accounts signed in at once. Each clone is a renamed,
# recolored, re-packaged copy of the stock Claude Android app, patched with the
# vantage-patches "Clone with badge" patch so it installs next to the original
# and next to the other clones. Package suffix words (two/three/four) match
# packages that already exist on a device - keep them exact.
CLAUDE_PACKAGE=com.anthropic.claude

CLAUDE_CLONES="2 3 4"
CLAUDE_CLONE_2_SUFFIX=two
CLAUDE_CLONE_2_LABEL="Claude 2"
CLAUDE_CLONE_2_ICON_COLOR="#0E7C7B"
CLAUDE_CLONE_3_SUFFIX=three
CLAUDE_CLONE_3_LABEL="Claude 3"
CLAUDE_CLONE_3_ICON_COLOR="#6B4FBB"
CLAUDE_CLONE_4_SUFFIX=four
CLAUDE_CLONE_4_LABEL="Claude 4"
CLAUDE_CLONE_4_ICON_COLOR="#1F6FB2"

# Claude ships as a split bundle (base + arm64_v8a + en + xxhdpi), no universal
# single APK - like X's APKM, but neither apkcombo nor uptodown serve a morphe-
# compatible APKM container, so download-apk.sh merges the splits into one APK
# via APKEditor before the gate runs (see the "bundle" container in
# download-apk.sh). APKMirror bot-blocks this app's pages entirely (not just the
# usual Cloudflare TLS check), so it is not in this list.
CLAUDE_DL_SOURCES="apkcombo uptodown"
CLAUDE_ARCH=arm64-v8a

# Play signing cert for com.anthropic.claude (base APK must carry this).
CLAUDE_EXPECTED_SIG_SHA256=305a1e8a432e5ec0c612b2465359c3b88e3c95d6253599ac6088562b818b64a0

# vantage-patches: the custom bundle carrying the "Clone with badge" patch
# (packageName / appLabel / badgeNumber / iconColor options). Floats to latest
# non-prerelease release, asset name pattern vantage-patches-<ver>.mpp. Override
# with VANTAGE_PATCHES_MPP=<local path> to use a local bundle instead.
VANTAGE_PATCHES_REPO=pmaxhogan/vantage-patches
VANTAGE_PATCHES_CHANNEL=release

# APKEditor merges Claude's split bundle into one APK before patching. Pinned
# version + sha256 (computed from the downloaded jar) so a compromised release
# asset can't slip in unnoticed.
APKEDITOR_VERSION=1.4.9
APKEDITOR_URL=https://github.com/REAndroid/APKEditor/releases/download/V${APKEDITOR_VERSION}/APKEditor-${APKEDITOR_VERSION}.jar
APKEDITOR_SHA256=a9cd40df818845456be6d696de6110c89edf4b0a0580cb83438ed6b25a366e67
